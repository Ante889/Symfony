{% extends 'base.html.twig' %}

{% block title %}Lista index{% endblock %}

{% block body %}
    <h1>Lista index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>City</th>
                <th>Phone</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for listum in listas %}
            <tr>
                <td>{{ listum.id }}</td>
                <td>{{ listum.name }}</td>
                <td>{{ listum.city }}</td>
                <td>{{ listum.phone }}</td>
                <td>
                    <a href="{{ path('lista_show', {'id': listum.id}) }}">show</a>
                    <a href="{{ path('lista_edit', {'id': listum.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('lista_new') }}">Create new</a>
{% endblock %}
