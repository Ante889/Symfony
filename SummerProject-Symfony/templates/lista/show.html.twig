{% extends 'base.html.twig' %}

{% block title %}Lista{% endblock %}

{% block body %}
    <h1>Lista</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ listum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ listum.name }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ listum.city }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ listum.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lista_index') }}">back to list</a>

    <a href="{{ path('lista_edit', {'id': listum.id}) }}">edit</a>

    {{ include('lista/_delete_form.html.twig') }}
{% endblock %}
